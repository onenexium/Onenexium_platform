/**
 * Product platform module — dashboard shell and future builder surfaces.
 * Implementation: `features/platform/*`.
 */
export { PlatformFrame, PlatformHeader, PlatformSidebar } from "@/features/platform/shell"
