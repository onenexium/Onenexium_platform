/**
 * Shared kernel — UI primitives, hooks, env, utilities.
 * Prefer `@shared/...` imports for cross-module code; keep features self-contained.
 */
export { cn } from "./lib/utils"
export { env } from "./lib/env"
